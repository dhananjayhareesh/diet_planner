import 'package:flutter/material.dart';

class FavRecipe extends StatelessWidget {
  const FavRecipe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Favorites')),
    );
  }
}
